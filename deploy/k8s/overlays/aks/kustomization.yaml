apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - gateway-ingress.yaml

patches:
  - path: gateway-lb.yaml
    target:
      kind: Service
      name: api-gateway
