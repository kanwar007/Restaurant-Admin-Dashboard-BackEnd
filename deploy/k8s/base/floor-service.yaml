apiVersion: v1
kind: Service
metadata:
  name: floor-service
spec:
  selector:
    app: floor-service
  ports:
    - port: 8083
      targetPort: 8083
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: floor-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: floor-service
  template:
    metadata:
      labels:
        app: floor-service
    spec:
      containers:
        - name: floor-service
          image: ghcr.io/kanwar007/restaurant-admin-dashboard-backend/floor-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: cafe-admin-config
          env:
            - name: SERVER_PORT
              value: "8083"
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: cafe-admin-db
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cafe-admin-db
                  key: password
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 384Mi
            limits:
              memory: 768Mi
