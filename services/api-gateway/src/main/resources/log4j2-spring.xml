<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
  <Properties>
    <Property name="SERVICE">${spring:spring.application.name:-cafe-admin}</Property>
    <Property name="PATTERN">%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [${SERVICE}] [%X{correlationId}] [%t] %logger{1.} - %msg%n%throwable</Property>
  </Properties>
  <Appenders>
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="${PATTERN}"/>
    </Console>
  </Appenders>
  <Loggers>
    <Logger name="com.cafeadmin" level="${env:APP_LOG_LEVEL:-INFO}"/>
    <Root level="${env:ROOT_LOG_LEVEL:-INFO}">
      <AppenderRef ref="Console"/>
    </Root>
  </Loggers>
</Configuration>
